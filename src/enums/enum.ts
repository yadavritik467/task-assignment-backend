export enum ROLES {
  USER = "user",
  ADMIN = "admin",
  MANAGER = "manager",
}
export enum STATUS {
  PENDING = "pending",
  DOING = "doing",
  DONE = "done",
}
export enum PRIORITY {
  LOW = "low",
  MEDIUM = "medium",
  HIGH = "high",
}

export enum SOCKETEVENT {
  TASK_CREATED = "TASK_CREATED",
  TASK_UPDATED = "TASK_UPDATED",
}
